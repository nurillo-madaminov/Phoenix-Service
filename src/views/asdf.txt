 <div>
         <div v-if="loading" class="flex justify-center items-center h-[20vh]">
        <span class="loading loading-dots loading-lg scale-150"></span>
      </div>
      <div v-if="!loading">
        <div>
          <div
            class="container grid grid-cols-3 gap-5 max-[1280px]:grid-cols-2 max-[768px]:grid-cols-1"
          >
            <div
              class="card bg-base-100 shadow-2xl border border-gray-200"
              v-for="(post, index) in posts"
              :key="index"
            >
              <!-- <figure>
                <img
                  src="https://picsum.photos/1980/"
                  alt="Shoes"
                  class="w-full h-[250px] bg-center"
                />
              </figure> -->
              <div class="card-body">
                <h2 class="card-title">{{ post.author }}</h2>
                <p>{{ posts.title }}</p>
                <div class="card-actions justify-end">
                  <router-link class="btn btn-primary">More</router-link>
                  <!-- :to="{ name: 'postDetails', params: { id: post.id } }" -->
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
      </div>